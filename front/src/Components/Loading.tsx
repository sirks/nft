import React from 'react';

const Loading = () => {
    return (
        <div className="flex relative justify-center mb-10">
            <span className="loader text-orange after:text-black" />
        </div>
    );
};

export default Loading;
